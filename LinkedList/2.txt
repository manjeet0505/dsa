Deleting a Node 
if(head==null) return head;
if(head.data == key){
    return head.next;
}
Node curr = head;
Node prev = null;

while(curr!=null){
    if(curr.data==key){
    prev.next = curr.next;
    return head;
    }

prev = curr;
curr = curr.next;
}
 return head;