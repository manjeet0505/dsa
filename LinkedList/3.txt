Deleting Nth Node From end 
if(head==null) return head;

Node slow = head;
Node Fast = head;

for(int i=0;i<n;i++){
    fast = fast.next;
}
if(fast.next == null){
    return head.next;
}

while(fast.next != null){
    slow = slow.next;
    fast = fast.next;
}

slow.next = slow.next.next;